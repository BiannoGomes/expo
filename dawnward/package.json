{
  "name": "dawnward",
  "private": true,
  "version": "0.1.0",
  "description": "BECOMING — Personal Evolution Intelligence. MVP monorepo.",
  "workspaces": [
    "packages/core",
    "server",
    "apps/mobile"
  ],
  "scripts": {
    "server": "npm run dev --workspace server",
    "mobile": "npm run start --workspace @dawnward/mobile",
    "migrate": "npm run migrate --workspace server",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
