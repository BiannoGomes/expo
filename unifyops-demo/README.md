# UnifyOps demo page — white-label hosting (3 minutes, do once)

The Wave follow-up drafts currently link to a **claude.ai artifact**. Verified 16 Sep:
an anonymous visitor hits the Claude shell + sign-in wall (the artifact is private),
and the domain itself reads as AI tooling. **Do not send follow-ups until the link is swapped.**

`index.html` in this folder is the exact AI Front Desk pitch page (personalisation via
`?biz=Clinic+Name` intact). Put it on a claude-free domain:

## Fastest free path — GitHub Pages (from your browser, logged in as BiannoGomes)
1. github.com → New repository → name it exactly `biannogomes.github.io` → Public → Create.
2. "uploading an existing file" → drop `index.html` from this folder → Commit.
3. Wait ~1 minute. The page is live at:
   `https://biannogomes.github.io/?biz=Calaway+Dental`
   (Settings → Pages if it hasn't auto-enabled: Source = main branch, / root.)

## Better (when you say the word): a real domain
`unifyops.pt` / `.com` (~€10/yr) pointed at the same Pages repo → `demo.unifyops.pt/?biz=…`.
Buying a domain = payment = your explicit yes; say it and the desktop session wires it.

## Then, before sending anything
Replace every `claude.ai/code/artifact/468eec30…` link in the HERMES drafts with the new
URL (keep each clinic's `?biz=` parameter). Two more things to eyeball in the drafts:
- The page prices **$500/mo** while EU outreach quotes **€** — one currency, your call.
- The guarantee ("5 booked appointments in 30 days or next month free") is a real
  promise — keep it only if you'll honour it.
